# Core: HydroGNN, UA-HydroGNN, Seq2Seq baselines, synthetic simulator
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0
torch>=2.1

# Optional: V-Seq2Seq variational baseline only. Uncomment to install.
# (Without these, `from seq2seq_runoff import VAESeq2SeqRunoffModel` will
# raise ImportError; everything else still works.)
# tensorflow>=2.13
# keras>=2.13
