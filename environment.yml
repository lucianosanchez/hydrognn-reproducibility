name: hydrognn
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python>=3.10,<3.13
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - pyyaml>=6.0
  - pytorch>=2.1
  - pip
  - pip:
      # V-Seq2Seq variational baseline only — comment out if you do not need it.
      - tensorflow>=2.13
      - keras>=2.13
